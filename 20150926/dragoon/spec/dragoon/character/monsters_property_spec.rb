require 'spec_helper'

describe Dragoon::Character::Monsters_property do
#  describe "level" do
#    let(:property) {Dragoon::Character::Property.new("dragoon")}
#
#    example "level 1" do
#      expect(property.name).to eq "dragoon"
#      expect(property.level).to eq 1
#      expect(property.attack).to eq 4
#      expect(property.defense).to eq 4
#      expect(property.hit_point).to eq 15
#      expect(property.magic_power).to eq 0
#    end
#  end
end
